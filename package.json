{
  "scripts": {
    "postinstall": "graphclient build --fileType json",
    "build": "graphclient build --fileType json",
    "start": "node --no-warnings ."
  },
  "dependencies": {
    "@graphprotocol/client-cli": "2.2.11",
    "graphql": "16.6.0"
  }
}
